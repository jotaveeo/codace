import { BrowserRouter, Route, Routes } from "react-router";

const Paths = () => {
    return (
        <>
        <BrowserRouter>
            <Routes>
                <Route path="/" element={/>
            </Routes>
        </BrowserRouter>
        </>
      );
}
 
export default Paths;