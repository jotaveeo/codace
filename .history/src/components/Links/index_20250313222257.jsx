const Links = ({ links }) => {

    const getLinkContent = (id) => {
        switch (id) {
               {getLinkContent(link.id)}
            <a key={link.id} href={link.path}>
               {switch (link.id) {
                case 1:
                    
                    break;
               
                default:
                    break;
               }}
                {link.name}
            </a>
        ))
      );
}
 
export default Links;